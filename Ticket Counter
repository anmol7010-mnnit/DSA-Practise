deque<int> dq;
        int ans;
        for(int i=1;i<=N;i++) dq.push_back(i);
        while(!dq.empty()){
            for(int i=0;i<K and !dq.empty();i++) {ans=dq.front(); dq.pop_front();}
            for(int i=0;i<K and !dq.empty();i++) {ans=dq.back();  dq.pop_back();}
        }
return ans;
